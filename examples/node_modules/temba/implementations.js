import { createGetStaticFileFromDisk, } from './staticFolder/staticFolder.js';
export const getDefaultImplementations = (config) => {
    const defaultImplementations = {
        getStaticFileFromDisk: createGetStaticFileFromDisk(config),
    };
    if (config.implementations) {
        return {
            ...defaultImplementations,
            ...config.implementations,
        };
    }
    return defaultImplementations;
};
//# sourceMappingURL=implementations.js.map